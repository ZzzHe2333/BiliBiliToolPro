name: Repository audit

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    name: Independent repository invariants
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Run repository audit
        shell: bash
        run: |
          set -euo pipefail
          python3 ./scripts/audit_repository.py
          python3 ./scripts/audit_hardening.py

      - name: Validate credential redaction behavior
        shell: bash
        run: |
          set -euo pipefail
          dotnet test ./test/BiliAgentTest/BiliAgentTest.csproj \
            --configuration Release \
            --filter 'FullyQualifiedName~SensitiveLogRedactorTest'

      - name: Validate Qinglong shell syntax
        shell: bash
        run: |
          set -euo pipefail
          for file in qinglong/SubscriptionTasks/*.sh qinglong/SubscriptionTasks/*.inc qinglong/DefaultTasks/*.inc; do
            bash -n "$file"
          done

      - name: Check changed-file whitespace
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            git fetch --no-tags origin "${{ github.base_ref }}"
            base="$(git merge-base HEAD "origin/${{ github.base_ref }}")"
            git diff --check "$base"...HEAD
          elif git rev-parse HEAD^ >/dev/null 2>&1; then
            git diff --check HEAD^ HEAD
          else
            git diff --check HEAD
          fi
