apiVersion: v1
kind: Service
metadata:
  name: {{ include "bilibili_tool.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "bilibili_tool.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bilibili_tool.selectorLabels" . | nindent 4 }}
